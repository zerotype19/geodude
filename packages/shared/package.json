{
  "name": "@geodude/shared",
  "version": "0.0.1",
  "type": "module",
  "main": "src/index.ts",
  "files": ["src"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"(todo lint)\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
