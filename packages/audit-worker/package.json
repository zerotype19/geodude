{
  "name": "optiview-audit-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 migrations apply optiview --remote",
    "migrate:local": "wrangler d1 migrations apply optiview --local"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241120.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.78.12"
  }
}
