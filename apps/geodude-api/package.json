{
  "name": "geodude-api",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "publish": "wrangler deploy",
    "lint": "echo \"(todo lint)\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@geodude/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250812.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.78.0"
  }
}