# API routes to external service (before SPA fallback)
/api/* https://api.optiview.ai/api/:splat 302

# SPA fallback: rewrite to index.html (no redirect)
# Cloudflare Pages treats "200" here as a rewrite, NOT a redirect.
# Keep this as the LAST rule.
/*   /index.html   200
