# Forward API requests to the API worker domain (MUST be first)
/api/* https://api.optiview.ai/api/:splat 302

# Simple SPA fallback - all other requests serve index.html for React Router
/* /index.html 200
