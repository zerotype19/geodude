# Forward API requests to the API worker domain (MUST be first)
/api/* https://api.optiview.ai/api/:splat 302

# SPA catch-all for React Router (MUST be last)
# This handles all frontend routes including client-side navigation
/* /index.html 200
